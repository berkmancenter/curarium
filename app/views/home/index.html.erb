<div class="container">

  <div class="row">
    <div class="col-md-2 visible-md">
      <%= image_tag 'animated_icon.gif', class: 'home-gif' %>
    </div>

    <div class="col-md-10 page-header">
      <p class="intro-blurb">Curarium leverages the power of the crowd to annotate, curate, &amp; augment works with the aim of constructing shareable, media-rich stories &amp; elaborate arguments about individual items as well as groups of items.</p>

      <p><b><%= link_to "Learn more", '/about'%></b></p>

      <% if authenticated? %>
        <p>To report a bug or suggestion, please visit the <a href="https://cyber.law.harvard.edu/projectmanagement/projects/curarium-beta-bug-tracker/issues/new">Curarium Bug Tracker</a></p>
      <% else %>
        <p class="home-login"><%= login_link 'SIGN UP' %> <span class="join">Join the Community!</span></p>
      <% end %>
    </div>
  </div>

  <h2 class="latest-activity">Latest Activity</h2> 

  <div class="row gallery">
    <% if @collection.present? %>
      <div class="col-sm-8 col-md-4 big">
        <%= render partial: 'collections/thumbnail', object: @collection %>
      </div>
    <% end %>

    <% @all.each do |item| %>
      <div class="col-xs-12 col-sm-4 col-md-2">
        <%= render partial: "#{item.class.to_s.pluralize(2)}/thumbnail", object: item %>
      </div>
    <% end %>
  </div>

</div>

<%= render partial: 'shared/beta_popup' %>
