<% if thumbnail.image.present? %>
  <%= render partial: 'works/thumbnail', object: thumbnail.image.work %>
<% elsif thumbnail.annotation.present? %>
  <%= render partial: 'works/thumbnail', object: thumbnail.annotation.work %>
<% end %>
