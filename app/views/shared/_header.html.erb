<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to raw( 'Curarium <sup>beta</sup>' ), root_url, class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="hidden-sm"><%= link_to render( partial: 'shared/mini_icon', object: 'about', locals: { side: :right } ), about_path %></li>
        <li><%= link_to render( partial: 'shared/mini_icon', object: 'collections', locals: { side: :right } ), collections_path %></li>
        <li><%= link_to render( partial: 'shared/mini_icon', object: 'spotlights', locals: { side: :right } ), spotlights_path %></li>
        <li class="dropdown">
        <%= link_to render( partial: 'shared/mini_icon', object: 'explore', locals: { side: :right } ), '#', class: 'dropdown-toggle', role: 'button', 'aria-haspopup' => 'true', 'aria-expanded' => 'false', data: { toggle: 'dropdown' } %>
          <ul class="dropdown-menu">
            <% @header_collections.each { |c| %>
              <li><%= link_to c.name, collection_works_path( c ) %></li>
            <% } %>
          </ul>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if authenticated? %>
          <li class="dropdown">

            <%= link_to render( partial: 'users/mini_icon', object: @current_user ), '#', class: 'dropdown-toggle', role: 'button', 'aria-haspopup' => 'true', 'aria-expanded' => 'false', data: { toggle: 'dropdown' } %>

            <ul class="dropdown-menu">
              <li><%= link_to 'View Profile', @current_user %></li>
              <li><%= link_to 'Edit Profile', edit_user_path( @current_user ) %></li>
              <li><%= link_to 'Sign Out', logout_path %></li>
            </ul>

          </li>

        <% else %>
          <li><%= link_to 'Sign In', new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
