<%= form_for([@owner, @owner.trays.build]) do |f| %>
  <% if @tray.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tray.errors.count, "error") %> prohibited this spotlight from being saved:</h2>

      <ul>
      <% @tray.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :records %><br>
    <%= f.text_field :records %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%= render 'shared/global' %>