<%= provide( :title, @collection.name ) unless @collection.nil? %>

<%=render partial: 'works/vis_controls', locals: { params: params } %>

<%= render partial: "works/#{params['vis'] || 'objectmap'}", object: @works %>
