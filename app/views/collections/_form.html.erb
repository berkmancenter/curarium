<div class="row">
  <div class="col-sm-12">
  </div>
</div>

<div class="row">

  <div class="col-xs-offset-1 col-xs-10 visible-xs">
    <div class="alert alert-info">
      <p>Please increase the size of your browser window or use a larger device to import collections. Thank you.</p>
    </div>
  </div>

  <%= semantic_form_for @collection, html: { multipart: true } do |f| %>

  <%= f.semantic_errors :collection %>

  <div class="col-sm-12 col-md-3 hidden-xs">
    <h2 class="h4 page-header">General Information</h2>

    <%= f.inputs { %>
      <%= f.input :approved, label: raw( '<b>Public</b> (uncheck to hide this collection from other users)' )  %>

      <%= f.input :name, placeholder: 'Choose a name for your collection', label: 'Collection Name' %>

      <%= f.input :description, as: :text, placeholder: 'Write a description for your collection. It can be pretty long.', label: 'Collection Description', input_html: { rows: 8 } %>

      <%= f.input :source, placeholder: 'Identify the provider of this collection', label: 'Collection Provider' %>

      <%= f.input :file, as: :file, label: 'ZIP of Collection JSON Files' %>

      <%= f.input :configuration, as: :hidden, input_html: { value: '{}' } %>
    <% } %>

    <%= f.actions { %>
      <%= f.action :submit, as: 'button', button_html: { class: 'btn btn-default btn-info pull-right' } %>
    <% } %>

  </div>

  <div class="col-sm-6 col-md-5 hidden-xs">
    <h2 class="h4 page-header">Collection JSON Sample</h2>
  </div>

  <div class="col-sm-6 col-md-4 hidden-xs">
    <h2 class="h4 page-header">Active Fields</h2>
  </div>

  <% end %>

</div>




<!--

<div class="uploader_content">  
  <div class="left">
    <div class="holder">
      <%#= form_for @collection, html: { :multipart => true } do |f| %>
        <%# if @collection.errors.any? %>
        <div id="error_explanation">
          <h2><%#= pluralize @collection.errors.count, 'error' %> prohibited this collection from being saved:</h2>

          <ul>
            <%# @collection.errors.full_messages.each do |msg| %>
            <li><%#= msg %></li>
            <%# end %>
          </ul>
        </div>
        <%# end %>
      
        <div class="titlebar">General Information</div> 

    
      <label for="json">Collection JSON Sample</label>
      <div class="input_box" data-intro="Copy and paste any individual JSON record from your collection here, as a sample" data-step="5">
        <textarea id="json" class="text" placeholder="Copy and paste your sample json record here"></textarea>
      </div>

      <span id="parse" class= "uploadjson" data-intro="Click this to see parsed data!" data-step="6">Parse</span>   
    </div>
  </div>
    
  <div class="expandable">
    <div class="holder">
      <div class="titlebar">Collection JSON Sample</div>

      <label>Parsed JSON Sample</label>
      <div class="json_holder" id="parsed" data-intro="Drag metadata values from the center column..." data-step="7">
        No Sample &ndash; Please paste JSON into the Collection JSON Sample box at the bottom left and click Parse
      </div>
    </div>
  </div>

  <div class="right">
    <div class="holder">
      <div class="titlebar" data-intro="...into their respective fields on the right column" data-step="8">Active Fields</div>

      <div id="output">
        <div id="unique_identifier" class="field field_wrapper">
          <p>Unique Identifier</p>
        </div>

        <div id="title" class="field field_wrapper">
          <p>Title</p>
        </div>

        <fieldset>
          <legend>Images</legend>
          <p class="note">Please use a URL to the actual image (sometimes called base image URL) rather than a URL that points to a web page for the record.</p>
          <p>If the collection has multiple (surrogate) images for a record, click the '0' dropdown list and choose '*' after dropping a sample URL.</p>

        <div id="image" class="field field_wrapper">
          <p>Image</p>
        </div>

        <div id="thumbnail" class="field field_wrapper">
          <p>Thumbnail</p>
        </div>

        </fieldset>

        <input id="daterange" type="checkbox">
        <label for="daterange" class="button">Date Range?</label>

        <div id="datestart" class="field field_wrapper">
          <p>Date</p>
        </div>

        <div id="dateend" class="field field_wrapper">
          <p>End Date</p>
        </div>

        <form id="add_field" data-intro="Add/Edit custom fields of your choice" data-step="9">
          <input id="field_name" name="field_name" type="text" placeholder="custom field name">
          <br>
          <input type="submit" value="Add field" id="submitbutton">
        </form>
      </div>
    </div>
  </div>
</div>
-->
