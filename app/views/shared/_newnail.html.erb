<% verb = verb || :new %>
<% target = target || nil %>

<%= link_to render( partial: 'shared/newnail_inner', object: newnail ), "#{new_polymorphic_path newnail}", target: target, class: 'gallery-item thumbnail newnail-thumbnail btn btn-default btn-lg' %>
