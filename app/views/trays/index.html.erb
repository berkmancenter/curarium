<% require 'works_helper' %>
<ul>
  <% @trays.each { |tray| %>
    <li>
      <b><%= tray.name %></b>
      <ul>
        <% tray.works.each { |record_id| %>
          <li><%= record_title Work.find( record_id ) %></li>
        <% } %>
      </ul>
    </li>
  <% } %>
</ul>
